apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mytasks-data
  namespace: mytasks
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
